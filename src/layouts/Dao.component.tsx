import styles from './Dao.module.scss';

const DAO = () => {
  return (
    <div className={styles["container"]}>
      <p className={styles['title']}>{'DAO 👷‍♂️🚧'}</p>
    </div>);
}

export default DAO;